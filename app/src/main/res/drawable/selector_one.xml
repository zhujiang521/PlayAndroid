<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 当前窗口失去焦点时 -->
    <item android:drawable="@drawable/ic_home" android:state_window_focused="false" />
    <!-- 不可用时 -->
    <item android:drawable="@drawable/ic_home" android:state_enabled="false" />
<!--    &lt;!&ndash; 按压时 &ndash;&gt;-->
<!--    <item android:drawable="@drawable/ic_nav_news_normal" android:state_pressed="true" />-->
    <!-- 被选中时 -->
    <item android:drawable="@drawable/ic_home_selected" android:state_selected="true" />
    <!-- 被激活时 -->
    <item android:drawable="@drawable/ic_home_selected" android:state_activated="true" />
    <!-- 默认时 -->
    <item android:drawable="@drawable/ic_home" />

</selector>